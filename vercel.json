{
  "version": 2,
  "builds": [
    { "src": "api/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/health", "dest": "/api/health.ts" },
    { "src": "/api/webhook", "dest": "/api/webhook.ts" },
    { "src": "/api/set-webhook", "dest": "/api/set-webhook.ts" }
  ]
}
